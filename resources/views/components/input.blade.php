<input 
        type="{{ $type }}" 
        id="{{ $id }}" 
        name="{{ $name }}"
        value="{{ $value }}"
        placeholder="{{ $placeholder }}"
        class="block w-full mt-2 p-2 border-b border-gray-400 {{ $class }}"/>
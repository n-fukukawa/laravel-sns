<textarea 
        required
        rows="16"
        id="{{ $id }}" 
        name="{{ $name }}"
        placeholder="{{ $placeholder }}"
        class="block w-full mt-2 p-2 border border-gray-400 {{ $class }}">{{ $value }}
</textarea>